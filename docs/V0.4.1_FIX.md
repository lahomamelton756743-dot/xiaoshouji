# 小手机 v0.4.1 修复

- 修复自定义 Android 构建脚本未把 `app/src/main/assets` 打进 APK 的问题。
- `LittlePhoneActivity` 的本地 Web/PWA 前端现在会随 APK 一起打包。
- 版本更新为 0.4.1（versionCode 40100）。
- 保留无障碍服务用于现有非截图设备能力，但显式关闭 `canTakeScreenshot`；截图方法仍为禁用状态。
