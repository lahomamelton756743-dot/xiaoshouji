# v0.5.1 R5

- 修复 `renderReminderSummary is not defined` 导致的前端启动链中断。
- readNative 的各渲染器互相隔离，单个 UI 模块报错不再阻断远端加载与其他功能。
- 纸条、普通信、重要日期、周期设置/记录改为本地优先：点击保存立即写入本机待同步队列，后台尝试 server，同步失败不阻塞 UI。
- 离线提示明确说明“本机保存仍可用，恢复后自动同步”。
- 日记封面提高对比度，避免玻璃高光把白字冲掉。
