# v0.5.2 MCP OAuth final compatibility fix

This overlay fixes ChatGPT MCP app creation with OAuth discovery.

Changes:
- MCP initialize / tools/list can be scanned before account linking.
- Every MCP tool advertises OAuth securitySchemes.
- tools/call remains protected and returns an MCP OAuth challenge when unlinked.
- Existing LINJIAN_TOKEN compatibility remains intact.
- DCR + PKCE OAuth flow remains supported.
- No frontend, Android, Netlify, or D1 data changes.

After applying this overlay, redeploy the existing Cloudflare Worker.
In ChatGPT Create MCP App:
- Server URL: https://little-phone-backend.lahomamelton756743.workers.dev/mcp
- Authentication: OAuth
- Advanced OAuth settings: leave all blank
