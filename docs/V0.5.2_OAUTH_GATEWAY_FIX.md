# v0.5.2 OAuth gateway fix

Problem: the browser cannot open `workers.dev/authorize` on the user's network and returns `ERR_TUNNEL_CONNECTION_FAILED`.

Fix:
- Keep the MCP resource endpoint on Cloudflare Worker: `https://little-phone-backend.lahomamelton756743.workers.dev/mcp`.
- Advertise the Netlify gateway as the OAuth authorization server.
- Route `/authorize`, `/token`, and `/register` through `https://little-phone-gateway.netlify.app`.
- Keep the same D1 OAuth clients/tokens and LINJIAN_TOKEN compatibility.
- No APK, frontend, or D1 data changes.
