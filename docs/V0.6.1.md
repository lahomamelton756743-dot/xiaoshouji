# v0.6.1 实施说明

本版遵循“在 v0.5.2 上升级、不推翻重做”的原则。

## 已实现

- 双人 profile（显示名 / 头像 / 身份色）与稳定 actor 键。
- 一起听 / 状态胶囊 / 耳机线 / 相伴天数的新“我们”页结构。
- App 使用前五名与等高足迹预览。
- 纸条 thread / reply。
- 信封式信箱、回信 thread、独立双方已拆状态。
- 未来信后端锁定与独立双方已拆状态。
- 日常册排序、裁切与原 ID 编辑。
- 待办过期 / 归档展示规则。
- `trigger_call`、真实 `scheduled_for`、popup/call 分离。
- 事件短窗口去重。
- 小手机液态玻璃应用门禁、申请解锁链路。
- Cloudflare phone control / snapshot state MCP 工具。

## 保留兼容

- 旧 `trigger_guidian` 仍可被 Android 识别，避免历史命令失效；新命令统一发 `trigger_call`。
- 旧单一 `seen` 字段保留迁移兼容，UI 与新接口优先使用 `user_seen` / `daddy_seen`。
- 现有真实数据不清库。

## 次日专项

2026-09-27 继续验证现有两封未来信到期后才可读取正文，并验证双方独立已拆状态。

## 构建说明

源码包已做 JS / Worker 语法检查和 Cloudflare 后端测试。Android APK 需要 Platform 34 / Build Tools 34.0.0 环境构建。
